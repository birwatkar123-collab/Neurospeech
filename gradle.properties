# Project-wide Gradle settings.
# Pin the Gradle JVM to a JDK 21 so the build is reproducible regardless of the
# machine default (JDK 25 is newer than what AGP 8.x is validated against).
org.gradle.java.home=C\:/Program Files/Eclipse Adoptium/jdk-21.0.12.101-hotspot
org.gradle.jvmargs=-Xmx2560m -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true

android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official